<!DOCTYPE html>
<html>
    <head>
        <title>YelpCamp</title>
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css">
        <link rel="stylesheet" href="/stylesheets/main.css">
    </head>
    <body>
        
        <nav class="navbar navbar-expand-lg navbar-default">
        <div class="container-fluid">
        <div class="navbar-header">
            <a style="color: black" class="navbar-brand" href="/">YelpCamp</a>
        </div>
        <div>
            <ul class="nav navbar-nav navbar-right" style="display: flex; justify-content:space-between;">
                    <% if(!currentUser){ %>
                    <li style="margin-right: 10px;"><a style="color: black" href="/login"> Login </a></li>
                    <li><a style="color: black" href="/register"> Sign Up </a></li>
                <% } else{ %>
                    <li><a style="color: black; margin-right: 10px" href="#">Signed In As <%= currentUser.username %></a></li>
                    <li><a style="color: black" href="/logout"> Log Out </a></li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>

<div class="container">
    <% if(error && error.length > 0) { %>
        <div class="alert alert-danger" role="alert">
            <%= error %>
        </div>
    <% } %>
    <% if(success && success.length > 0) { %>
        <div class="alert alert-success" role="alert">
            <%= success %>
        </div>
    <% } %>
       
</div>





